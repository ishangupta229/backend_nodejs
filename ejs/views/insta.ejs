<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Insta</title>
    <link rel="stylesheet" href="../css/style.css">
</head>
<body>
    <h3>
        <%= data.name %>, we welcome you. <button> Follow</button> &nbsp <button>message</button>
    </h3>
    <p>
        Follower = <%= data.followers  %>  &nbsp;&nbsp;&nbsp;&nbsp;
        Following = <%= data.following  %> 
    </p>

    <hr>

    <div>
        <% for(let post of data.posts) { %>
            <div>
                <img id="ew" src="<%= post.image %>" alt="" srcset="">
                <p>
                    Likes = <%= post.likes  %>  <br>
                    Comments = <%= post.comments  %> 
                </p>
            </div>
        <% } %>

    </div>
</body>
<script src="../js/script.js"></script>
</html>